<?
/*
// сортировка по убыванию(свойство "COUNTER_OF_VIEWINGS")
 for ($i=0; $i < count($arResult["ITEMS"]); $i++)
    {
        for ($y=($i+1); $y < count($arResult["ITEMS"]); $y++)
        {
            if ($arResult["ITEMS"][$i]["P"]["COUNTER_OF_VIEWINGS"]["VALUE"] < $arResult["ITEMS"][$y]["P"]["COUNTER_OF_VIEWINGS"]["VALUE"])
            {
                $c = $arResult["ITEMS"][$i];
                $arResult["ITEMS"][$i] = $arResult["ITEMS"][$y];
                $arResult["ITEMS"][$y] = $c;
            }
        }
    }
*/